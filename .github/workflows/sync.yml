name: Sync with Parent

# ─────────────────────────────────────────────────────────
# Triggers:
#   1. Cron: Saatte bir upstream kontrolü
#   2. repository_dispatch: Parent push yaptığında
#   3. workflow_dispatch: Manuel tetikleme
# ─────────────────────────────────────────────────────────
on:
  schedule:
    - cron: "0 * * * *"  # Her saat başı

  repository_dispatch:
    types: [upstream-sync]

  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run — merge etme, sadece kontrol et"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

env:
  UPSTREAM_REPO: "https://github.com/Giorino/sync-parent.git"
  UPSTREAM_BRANCH: "main"
  TARGET_BRANCH: "main"

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    name: Sync upstream changes
    runs-on: ubuntu-latest

    steps:
      # ──────────────────────────────────────
      # 1. Checkout — full history gerekli
      # ──────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.SYNC_PAT }}

      # ──────────────────────────────────────
      # 2. Git identity ayarla
      # ──────────────────────────────────────
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Remote URL'i PAT ile güncelle (workflow dosyalarını push edebilmek için)
          git remote set-url origin "https://x-access-token:${{ secrets.SYNC_PAT }}@github.com/${{ github.repository }}.git"

      # ──────────────────────────────────────
      # 3. Upstream remote ekle ve fetch et
      # ──────────────────────────────────────
      - name: Add upstream remote
        run: |
          git remote add upstream "${{ env.UPSTREAM_REPO }}" 2>/dev/null || true
          git fetch upstream
          echo ""
          echo "📊 Upstream status:"
          echo "  Local HEAD:    $(git rev-parse HEAD)"
          echo "  Upstream HEAD: $(git rev-parse upstream/${{ env.UPSTREAM_BRANCH }})"

      # ──────────────────────────────────────
      # 4. Değişiklik var mı kontrol et
      # ──────────────────────────────────────
      - name: Check for changes
        id: check
        run: |
          LOCAL_SHA=$(git rev-parse HEAD)
          UPSTREAM_SHA=$(git rev-parse upstream/${{ env.UPSTREAM_BRANCH }})

          if [ "$LOCAL_SHA" = "$UPSTREAM_SHA" ]; then
            echo "✅ Already up to date — nothing to sync."
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            BEHIND=$(git rev-list --count HEAD..upstream/${{ env.UPSTREAM_BRANCH }})
            if [ "$BEHIND" -eq 0 ]; then
              echo "✅ Upstream has no new commits — nothing to sync."
              echo "has_changes=false" >> $GITHUB_OUTPUT
            else
              echo "🔄 $BEHIND commit(s) behind upstream."
              echo "has_changes=true" >> $GITHUB_OUTPUT
              echo "behind_count=$BEHIND" >> $GITHUB_OUTPUT
            fi
          fi

      # ──────────────────────────────────────
      # 5. Dry run modu (sadece kontrol)
      # ──────────────────────────────────────
      - name: Dry run check
        if: steps.check.outputs.has_changes == 'true' && github.event.inputs.dry_run == 'true'
        run: |
          echo "🧪 DRY RUN — Merge simülasyonu:"
          if git merge --no-commit --no-ff --allow-unrelated-histories upstream/${{ env.UPSTREAM_BRANCH }} 2>/dev/null; then
            echo "  ✅ Clean merge mümkün"
            git merge --abort 2>/dev/null || true
          else
            echo "  ⚠️  Conflict tespit edildi!"
            git merge --abort 2>/dev/null || true
            echo ""
            echo "Conflict'li dosyalar:"
            git diff --name-only --diff-filter=U 2>/dev/null || true
          fi
          echo ""
          echo "ℹ️  Dry run tamamlandı — değişiklik uygulanmadı."

      # ──────────────────────────────────────
      # 6. Sync branch oluştur ve merge dene
      # ──────────────────────────────────────
      - name: Create sync branch and attempt merge
        id: merge
        if: steps.check.outputs.has_changes == 'true' && github.event.inputs.dry_run != 'true'
        run: |
          TIMESTAMP=$(date -u +%Y%m%d-%H%M%S)
          BRANCH_NAME="upstream-sync/${TIMESTAMP}"
          echo "branch_name=$BRANCH_NAME" >> $GITHUB_OUTPUT

          echo "🌿 Creating sync branch: $BRANCH_NAME"
          git checkout -b "$BRANCH_NAME"

          echo "🔀 Merging upstream/${{ env.UPSTREAM_BRANCH }}..."

          if git merge upstream/${{ env.UPSTREAM_BRANCH }} \
              --no-edit \
              --allow-unrelated-histories \
              -m "chore: sync with upstream ($(date -u +%Y-%m-%dT%H:%M:%SZ))"; then
            echo ""
            echo "✅ Merge successful — no conflicts."
            echo "merge_status=clean" >> $GITHUB_OUTPUT
          else
            echo ""
            echo "⚠️ Merge has conflicts — committing conflict markers."
            echo "merge_status=conflict" >> $GITHUB_OUTPUT

            # Conflict'li dosyaları kaydet
            CONFLICTED_FILES=$(git diff --name-only --diff-filter=U | tr '\n' ',' | sed 's/,$//')
            echo "conflicted_files=$CONFLICTED_FILES" >> $GITHUB_OUTPUT
            echo "Conflicted files: $CONFLICTED_FILES"

            # Conflict marker'lı dosyaları commit et
            git add -A
            git commit -m "chore: upstream sync conflict (needs manual resolution)

          Conflicted files: ${CONFLICTED_FILES}

          This branch contains merge conflicts from upstream that need
          manual resolution. Please resolve the conflicts and merge this PR."
          fi

          # Branch'i push et
          echo ""
          echo "🚀 Pushing branch..."
          git push origin "$BRANCH_NAME"
          echo "✅ Branch pushed."

      # ──────────────────────────────────────
      # 7. Label'ları oluştur (yoksa)
      # ──────────────────────────────────────
      - name: Ensure labels exist
        if: steps.merge.outputs.merge_status == 'clean' || steps.merge.outputs.merge_status == 'conflict'
        env:
          GH_TOKEN: ${{ secrets.SYNC_PAT }}
        run: |
          gh label create "auto-sync" \
            --description "Automatically created by upstream sync" \
            --color "0E8A16" 2>/dev/null || true

          gh label create "conflict" \
            --description "Merge conflict needs manual resolution" \
            --color "D93F0B" 2>/dev/null || true

          gh label create "ready-to-merge" \
            --description "Clean sync — ready to merge" \
            --color "0075CA" 2>/dev/null || true

      # ──────────────────────────────────────
      # 8. Clean merge → PR oluştur (review için)
      # ──────────────────────────────────────
      - name: Create clean sync PR
        if: steps.merge.outputs.merge_status == 'clean'
        env:
          GH_TOKEN: ${{ secrets.SYNC_PAT }}
        run: |
          BRANCH_NAME="${{ steps.merge.outputs.branch_name }}"
          BEHIND_COUNT="${{ steps.check.outputs.behind_count }}"

          PR_BODY="## ✅ Upstream Sync — Clean Merge

          Upstream repository'den gelen değişiklikler başarıyla merge edildi. Conflict yok.

          ### 📊 Detaylar

          | | |
          |---|---|
          | **Upstream** | \`${{ env.UPSTREAM_REPO }}\` |
          | **Commits** | ${BEHIND_COUNT} yeni commit |
          | **Tarih** | $(date -u +%Y-%m-%d\ %H:%M:%S\ UTC) |
          | **Tetikleyen** | \`${{ github.event_name }}\` |
          | **Durum** | ✅ Clean merge — conflict yok |

          ### 🔍 Review

          Değişiklikleri inceleyip uygunsa merge edebilirsiniz.

          ---
          _Bu PR otomatik olarak GitHub Actions tarafından oluşturulmuştur._"

          PR_URL=$(gh pr create \
            --base "${{ env.TARGET_BRANCH }}" \
            --head "$BRANCH_NAME" \
            --title "✅ Upstream Sync — ${BEHIND_COUNT} commit" \
            --body "$PR_BODY")

          echo "PR created: $PR_URL"

          # Label'ları ekle
          gh pr edit "$BRANCH_NAME" --add-label "auto-sync" --add-label "ready-to-merge" || true

          echo "✅ Clean sync PR created — ready for review."

      # ──────────────────────────────────────
      # 9. Conflict → PR oluştur (çözüm için)
      # ──────────────────────────────────────
      - name: Create conflict PR
        if: steps.merge.outputs.merge_status == 'conflict'
        env:
          GH_TOKEN: ${{ secrets.SYNC_PAT }}
        run: |
          CONFLICTED_FILES="${{ steps.merge.outputs.conflicted_files }}"
          BRANCH_NAME="${{ steps.merge.outputs.branch_name }}"
          BEHIND_COUNT="${{ steps.check.outputs.behind_count }}"

          # Dosya listesini markdown formatına çevir
          FILE_LIST=$(echo "$CONFLICTED_FILES" | tr ',' '\n' | sed 's/^/- `/' | sed 's/$/`/')

          PR_BODY="## ⚠️ Upstream Sync Conflict

          Upstream repository'den gelen değişiklikler ile bu repository arasında conflict tespit edildi.

          ### 📊 Detaylar

          | | |
          |---|---|
          | **Upstream** | \`${{ env.UPSTREAM_REPO }}\` |
          | **Behind** | ${BEHIND_COUNT} commit |
          | **Tarih** | $(date -u +%Y-%m-%d\ %H:%M:%S\ UTC) |
          | **Tetikleyen** | \`${{ github.event_name }}\` |

          ### 📝 Conflict'li Dosyalar

          ${FILE_LIST}

          ### 🔧 Çözüm Adımları

          1. Bu branch'i local'e çekin:
             \`\`\`bash
             git fetch origin
             git checkout ${BRANCH_NAME}
             \`\`\`

          2. Conflict'leri çözün:
             \`\`\`bash
             # Her conflict'li dosyayı düzenleyin
             # <<<<<<< HEAD ve >>>>>>> upstream/main marker'larını kaldırın
             git add <resolved-files>
             git commit -m \"fix: resolve upstream sync conflicts\"
             \`\`\`

          3. PR'ı merge edin.

          ---
          _Bu PR otomatik olarak GitHub Actions tarafından oluşturulmuştur._"

          PR_URL=$(gh pr create \
            --base "${{ env.TARGET_BRANCH }}" \
            --head "$BRANCH_NAME" \
            --title "⚠️ Upstream Sync Conflict — Manuel Çözüm Gerekli" \
            --body "$PR_BODY")

          echo "PR created: $PR_URL"

          # Label'ları ekle
          gh pr edit "$BRANCH_NAME" --add-label "auto-sync" --add-label "conflict" || true

          echo "✅ Conflict PR created — manual resolution needed."

      # ──────────────────────────────────────
      # 10. Slack bildirim (opsiyonel)
      # ──────────────────────────────────────
      - name: Notify Slack (optional)
        if: steps.merge.outputs.merge_status == 'conflict' && env.SLACK_WEBHOOK_URL != ''
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -s -X POST "$SLACK_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -d "{
              \"text\": \"⚠️ *Upstream Sync Conflict*\",
              \"blocks\": [
                {
                  \"type\": \"header\",
                  \"text\": {
                    \"type\": \"plain_text\",
                    \"text\": \"⚠️ Upstream Sync Conflict\"
                  }
                },
                {
                  \"type\": \"section\",
                  \"fields\": [
                    {\"type\": \"mrkdwn\", \"text\": \"*Repo:*\n${{ github.repository }}\"},
                    {\"type\": \"mrkdwn\", \"text\": \"*Files:*\n${{ steps.merge.outputs.conflicted_files }}\"}
                  ]
                },
                {
                  \"type\": \"section\",
                  \"text\": {
                    \"type\": \"mrkdwn\",
                    \"text\": \"<${{ github.server_url }}/${{ github.repository }}/pulls|View Pull Requests>\"
                  }
                }
              ]
            }"
          echo "✅ Slack notification sent."

      # ──────────────────────────────────────
      # 11. Özet
      # ──────────────────────────────────────
      - name: Summary
        if: always()
        run: |
          echo ""
          echo "═══════════════════════════════════════"
          echo "  Upstream Sync Summary"
          echo "═══════════════════════════════════════"
          echo ""

          if [ "${{ steps.check.outputs.has_changes }}" = "false" ]; then
            echo "  Status: ✅ Already up to date"
          elif [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            echo "  Status: 🧪 Dry run completed"
          elif [ "${{ steps.merge.outputs.merge_status }}" = "clean" ]; then
            echo "  Status: ✅ Clean sync PR created"
            echo "  Branch: ${{ steps.merge.outputs.branch_name }}"
            echo "  Commits: ${{ steps.check.outputs.behind_count }}"
            echo "  Action: Review and merge the PR"
          elif [ "${{ steps.merge.outputs.merge_status }}" = "conflict" ]; then
            echo "  Status: ⚠️ Conflict PR created"
            echo "  Branch: ${{ steps.merge.outputs.branch_name }}"
            echo "  Files:  ${{ steps.merge.outputs.conflicted_files }}"
            echo "  Action: Resolve conflicts and merge the PR"
          else
            echo "  Status: ⏭️ Skipped"
          fi

          echo ""
          echo "═══════════════════════════════════════"
